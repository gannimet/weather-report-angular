import { WindSpeedKphPipe } from './wind-speed-kph.pipe';

describe('WindSpeedKphPipe', () => {
  it('create an instance', () => {
    const pipe = new WindSpeedKphPipe();
    expect(pipe).toBeTruthy();
  });
});
